define([], function() {
    return {
        /** We've started a Standard Login - i.e. with Username and Password */
        STANDARD_LOGIN: 'LOGIN',
        /** We've successfully logged in */
        LOGIN_SUCCESS: 'LOGIN_SUCCESS',
        /** We've failed to log in for some reason */
        LOGIN_FAILURE: 'LOGIN_FAILURE'
    };
});
